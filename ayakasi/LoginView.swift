import SwiftUI

struct LoginView : View {
    var body: some View {
        ZStack(alignment: .bottom){
            Text("ssss")
            
            Image("yukiicon")
                .resizable()
                .aspectRatio(contentMode: .fit)
        }
    }
}


#Preview {
    LoginView()
}
